import UserProfile from '@/component/usersProfile';
import React from 'react';

const page = () => {
    return (
        <div>
            <UserProfile></UserProfile>
        </div>
    );
};

export default page;